package sumofdigits;
import java.util.Scanner;
public class PlayCarrom {
	 Scanner sc=new Scanner(System.in);
	   String team=sc.nextLine(); 
	   int n=team.length();
	   char[] ch=new char[n];
	   int count=0;
	   
	   
	   for(int i=0;i<n;i++) {
		   
		   ch[i]=team.charAt(i);
		  System.out.print(ch[i]);
	   }
	   
		   if(ch[0]=='A'||ch[0]=='C'||ch[4]==1) {
			   if(ch[2]==1&&ch[2]<=9&&ch[6]<=1) {
				   while(ch[4]<=2) {
			   count=count+ch[2]+ch[6];
			   System.out.println(count);
		   }
		   }
		   }
		   else if(ch[0]=='B'||ch[0]=='D'||ch[4]==1) {
			   if(ch[2]==1&&ch[2]<=9&&ch[6]<=1) {
				   while(ch[4]<=2) {
			   count=count+ch[2]+ch[6];
			   System.out.println(count);
		   
	   }
			   }
			   }
		   else if(ch[0]=='#'){
			   System.out.println("Incomplete game");
		   }


}
}